name: 02 - Workflow Events
on: 
    push:
    schedule:
        - cron: '*/5 * * * *'
    workflow_dispatch:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps: 
            - name: Show the trigger
              run: echo echo "${{ github.event_name }}"